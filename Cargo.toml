[workspace]
members = [
  "apps/desktop",
  "spikes/tailscale-web-push",
  "spikes/codex-rate-limits",
]
resolver = "3"

[workspace.package]
edition = "2024"
rust-version = "1.98"
license = "MIT OR Apache-2.0"
publish = false

[profile.release]
strip = "symbols"
lto = "thin"
