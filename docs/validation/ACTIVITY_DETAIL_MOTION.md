# Activity detail motion — 2026-09-04

The requested small refinement adds one decorative three-bar signal beside the selected session's working status. Full motion uses a 2.4-second transform/opacity cycle; Balanced, Minimal and system reduced motion retain the static mark. Waiting, terminal, offline and expired session evidence remove it. Hidden documents and offscreen content cancel the loop. The existing timeline and final-answer flow are preserved.

Validation: 104 Angular tests, 142 Rust workspace tests, 116 production Playwright scenarios and six Gate 0 helper tests passed. Formatting, lint, type checking, generated contracts, release/Tailwind builds, architecture, copy and hygiene checks passed. Browser coverage includes stable text geometry, lifecycle transitions, motion settings, hide/resume, offscreen scrolling, Ionic navigation and stale expiry. Twelve light/dark captures at 320/390/1024 px and completion/offline/reduced states were reviewed. Impeccable disposition: **ship**; both detector advisories concern inherited markup outside this change. DESIGN.md and its sidecar record the narrow rule.

The final release is **1.1.2**. Its production/Tailwind build and regenerated API contract passed, followed by eight focused light/dark detail-motion checks on that exact build. The clean Windows package smoke passed extraction, lifecycle, private health, PWA assets, REST, SSE, allowance, synthetic activity, delayed queue and persistence without developer runtimes. A real service-worker update from the published 1.1.1 executable showed the 1.1.2 notice, required explicit activation and retained local data through the new offline shell.

The nine-file Windows ZIP is `VibePing-Windows-x64-v1.1.2.zip`, SHA-256 `3122edb49e1da1ad3c055ba89cb1cd473c27d06e07f9f4b3ff3098f693d2b15e`. The previous 1.1.1 package is preserved. Upgrade instructions now explicitly disable the companion before stopping the host so its executable is released before replacement.

The final package was applied to the existing stable installation after backing up all package files and protected data. Local and private health report 1.1.2; the private origin's service-worker hash table matches the release build. A new companion heartbeat is healthy, with tray and the existing Windows sign-in path restored. Checks after startup verified all fifteen previously stored final answers, owner pairing, two subscription identities and VAPID identity unchanged. Private Tailscale Serve is unchanged and Funnel remains off.

RustSec passed with the existing reviewed `RUSTSEC-2023-0071` exception; the ES256-only source and transitive dependency path were checked. The production npm audit could not complete: its legacy endpoint repeatedly timed out, and the bulk endpoint returned HTTP 503 or timed out. No dependency versions changed; the previous 1.1.1 validation records a successful npm audit on the same date. This is an unavailable fresh JavaScript advisory check, not a clean audit result. The release fixes native exit-code propagation in `check-dependencies.ps1`: Windows PowerShell 5.1 now fails when npm, dependency-tree inspection or RustSec fails. An actual timed-out npm request verified the nonzero exit and absence of the success message.

Runtime fingerprints, backups and captures stay outside Git. Physical iPhone rendering and a Windows sign-out/sign-in were not performed. Existing initial-bundle and source-length warnings remain below their failure limits.
